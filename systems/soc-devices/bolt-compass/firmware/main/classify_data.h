/*
 * classify_data.h — (empty: weights are in classify_data.c)
 * Kept for symmetry; the classifier API is in classify.h.
 */
#ifndef BOLT_COMPASS_CLASSIFY_DATA_H
#define BOLT_COMPASS_CLASSIFY_DATA_H
#endif